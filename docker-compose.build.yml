version: "3.9"

# Build image locally instead of pulling from registry.
# Usage: docker-compose -f docker-compose.yml -f docker-compose.build.yml up --build

services:
  app:
    image: resume-builder:local
    build:
      context: .
      dockerfile: Dockerfile
